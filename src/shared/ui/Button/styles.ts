import { CSSProperties } from "react";

import { LINE_HEIGHT, TITLE_WEIGHT } from "@/shared/styles";

export const buttonStyles: CSSProperties = {
    padding: 12,
    height: 50,
    width: "100%",
    fontSize: 16,
    fontWeight: TITLE_WEIGHT,
    lineHeight: LINE_HEIGHT,
};
